# -*- coding: utf-8 -*-
"""main.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1ZADZPyynwLET-DbxyxAuqQl69fdsig78
"""



# Commented out IPython magic to ensure Python compatibility.
# %%writefile app.py
# import streamlit as st
# import joblib
# import pandas as pd
# 
# # Load the trained model
# model = joblib.load('random_forest_model.joblib')
# 
# st.title('Heart Disease Prediction App')
# st.write('Enter the patient information below to predict the likelihood of heart disease.')
# 
# # Input fields for features
# age = st.number_input('Age', min_value=29, max_value=77, value=50)
# sex = st.selectbox('Sex', options=[0, 1], format_func=lambda x: 'Female' if x == 0 else 'Male')
# cp = st.selectbox('Chest Pain Type (cp)', options=[0, 1, 2, 3], format_func=lambda x: {0: 'Typical Angina', 1: 'Atypical Angina', 2: 'Non-Anginal Pain', 3: 'Asymptomatic'}.get(x, x))
# trestbps = st.number_input('Resting Blood Pressure (trestbps)', min_value=94, max_value=200, value=120)
# chol = st.number_input('Cholesterol (chol)', min_value=126, max_value=564, value=200)
# fbs = st.selectbox('Fasting Blood Sugar > 120 mg/dl (fbs)', options=[0, 1], format_func=lambda x: 'False' if x == 0 else 'True')
# restecg = st.selectbox('Resting Electrocardiographic Results (restecg)', options=[0, 1, 2], format_func=lambda x: {0: 'Normal', 1: 'ST-T Wave Abnormality', 2: 'Left Ventricular Hypertrophy'}.get(x, x))
# thalach = st.number_input('Maximum Heart Rate Achieved (thalach)', min_value=71, max_value=202, value=150)
# exang = st.selectbox('Exercise Induced Angina (exang)', options=[0, 1], format_func=lambda x: 'No' if x == 0 else 'Yes')
# oldpeak = st.number_input('ST Depression Induced by Exercise Relative to Rest (oldpeak)', min_value=0.0, max_value=6.2, value=1.0, format='%.1f')
# slope = st.selectbox('Slope of the Peak Exercise ST Segment (slope)', options=[0, 1, 2], format_func=lambda x: {0: 'Upsloping', 1: 'Flat', 2: 'Downsloping'}.get(x, x))
# ca = st.selectbox('Number of Major Vessels Colored by Fluoroscopy (ca)', options=[0, 1, 2, 3, 4], format_func=lambda x: str(x) if x != 4 else 'NA (Missing value or error)')
# thal = st.selectbox('Thalassemia (thal)', options=[0, 1, 2, 3], format_func=lambda x: {0: 'NA (Missing value or error)', 1: 'Fixed Defect', 2: 'Normal', 3: 'Reversible Defect'}.get(x, x))
# 
# # Create a DataFrame from inputs
# input_data = pd.DataFrame([[age, sex, cp, trestbps, chol, fbs, restecg, thalach, exang, oldpeak, slope, ca, th]],
#                           columns=['age', 'sex', 'cp', 'trestbps', 'chol', 'fbs', 'restecg', 'thalach', 'exang', 'oldpeak', 'slope', 'ca', 'thal'])
# 
# # Prediction button
# if st.button('Predict'):
#     prediction = model.predict(input_data)
#     prediction_proba = model.predict_proba(input_data)
# 
#     st.subheader('Prediction Result:')
#     if prediction[0] == 1:
#         st.success(f'The model predicts: **Heart Disease Likely**')
#     else:
#         st.info(f'The model predicts: **No Heart Disease**')
# 
#     st.write(f'Probability of No Heart Disease: {prediction_proba[0][0]:.2f}')
#     st.write(f'Probability of Heart Disease: {prediction_proba[0][1]:.2f}')

